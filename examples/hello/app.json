{
    "name": "functions-framework-php-hello",
    "env": {
        "FUNCTION_TARGET": {
            "description": "the function name to call (you probably don't need to change this)",
            "value": "helloHttp",
            "required": true
        },
        "FUNCTION_SIGNATURE_TYPE": {
            "description": "the function signature type (you probably don't need to change this)",
            "value": "http",
            "required": true
        }
    }
}
